const strings = [1, 2, 3, 4, 5, 6];

// push - at the end
strings.push(7);

// pop - remove from the end
strings.pop();

// unshift - add at start
strings.unshift(0);

// shift - remove from start
strings.shift();

// accessing the array
strings[0];

// splice 

// slice





